---
name: improve-analysis
description: Run one bounded daily improvement session on the StockThink analysis engine. Use when the user says "improve analysis", "train", "daily improvement", or similar.
---

# Daily improvement session

Open `improve/README.md` and follow it EXACTLY. That file is the complete,
self-contained protocol: orientation (TRACKER.md + TODO.md), hard budget limits
(default 3 / max 5 work units), per-item procedure, the quality gate, and the
mandatory end-of-session bookkeeping + commit + **git push** (Pages deploy).

Do not read beyond what the protocol tells you to read — the workshop is
deliberately small so sessions start fast and never overflow context.
